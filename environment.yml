# 📦 Environment file for AI Task Allocator
# Run: conda env create -f environment.yml

name: ai-task-allocator
channels:
  - conda-forge
dependencies:
  - python=3.10          # 🎯 Stable, modern Python version
  - pulp=2.7.0           # 🧮 LP solver for optimization
  - pandas=2.2.2         # 📊 For data handling
  - matplotlib=3.8.4     # 📈 For result visualizations

  # 🧠 Optional Enhancements (Uncomment if needed)
  # - streamlit=1.35.0   # 🌐 For building a web app version
  # - seaborn=0.13.2     # 🎨 Beautiful visualizations
  # - openpyxl=3.1.2     # 📁 Excel file support

# 💡 After creation:
# conda activate ai-task-allocator
# jupyter notebook
